# Public catalog boundary

The public client must never receive procurement source, supplier identity, supplier URL, acquisition cost, internal margin, source SKU, or private availability notes. Those fields belong in a separate private system and are intentionally absent from the public catalog contract.
